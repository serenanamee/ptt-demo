

<h1>Edit</h1>

<%= form_with(model: @post, local: true) do |f| %>

<div class="fields">
<%= f.label :title, "看板名稱" %>
<%= f.text_field :title%>
</div>

<div class="fields">
<%= f.label :intro, "看板說明" %>
<%= f.text_area :intro%>
</div>
<%= f.submit %>

<%end%>

