<h1>看板 <%= @board.title %></h1>
<div>
  [版主: <%= display_bm(@board)%>]
</div>
<%= link_to "New", new_board_post_path(@board)%>
<ul>
  <% @posts.each do |post|%>
  <li>
    <%= post.title%>
    <%= link_to' edit', edit_post_path(post)%>
  </li>
  <%end%>
</ul>