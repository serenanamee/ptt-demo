hello

<a href="/boards/new">new</a>

<a href="<%= new_board_path %>">new</a>

<%=link_to "new", new_board_path, class: "abc", id: "xyz" %>

<ul>
  <% @boards.each do |board| %>
  <li><%= link_to board.title, board_path(board)%>
      <%= link_to "edit" ,edit_board_path(board)%>
  </li>
  <% end %>
</ul>
 
